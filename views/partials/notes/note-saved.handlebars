
          <li class="list-group-item d-flex justify-content-between align-items-center" data-id="{{_id}}">
              {{body}}
            <button type="button" class="deleteNote" data-id="{{_id}}" aria-label="Close">
              <span aria-hidden="true">&times;</span>
            </button>
          </li>
